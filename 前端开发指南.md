# 人脸识别与表情识别系统 - 前端开发指南

## 🎯 项目概述

这是一个基于深度学习的人脸识别与表情识别系统后端服务，为前端开发人员提供完整的API接口。

## 🚀 快速开始

### 1. 环境要求
- Python 3.8+
- 支持CUDA的GPU（可选，用于加速）

### 2. 安装依赖
```bash
pip install -r requirements.txt
```

### 3. 启动服务
```bash
python start_server.py
```

服务将在 `http://127.0.0.1:8000` 启动

## 📚 API文档

详细的API接口文档请查看：[API接口文档.md](./API接口文档.md)

### 核心接口
- `POST /face/recognize_image` - 人脸识别（图片）
- `POST /emotion/recognize_image` - 表情识别（图片）
- `POST /combined/recognize_image` - 综合识别（图片）
- `POST /face/recognize_video` - 人脸识别（视频）
- `POST /emotion/recognize_video` - 表情识别（视频）
- `POST /combined/recognize_video` - 综合识别（视频）

### WebSocket实时流接口
- `ws://127.0.0.1:8000/face/recognize_rtsp` - RTSP流人脸识别
- `ws://127.0.0.1:8000/face/recognize_camera` - 本地摄像头人脸识别
- `ws://127.0.0.1:8000/emotion/recognize_rtsp` - RTSP流表情识别
- `ws://127.0.0.1:8000/emotion/recognize_camera` - 本地摄像头表情识别
- `ws://127.0.0.1:8000/combined/recognize_stream` - 综合识别实时流

## 🎨 前端示例

### 1. 在线演示
访问 `http://127.0.0.1:8000/frontend` 查看完整的前端演示

### 2. 前端示例文件
- `frontend_demo.html` - 完整的前端演示页面

## ⚙️ 配置说明

系统配置请查看：[配置使用说明.md](./配置使用说明.md)

### 主要配置项
- 识别阈值设置
- 性能参数调整
- 模型路径配置

## 🔧 调试工具

### 系统状态检查
```bash
GET /health
```

### 调试接口
- `GET /debug/features` - 查看特征数据库
- `GET /debug/device_status` - 查看设备状态
- `GET /debug/test_extraction` - 测试特征提取

## 📝 使用示例

### JavaScript文件上传示例
```javascript
async function uploadImage(file) {
  const formData = new FormData();
  formData.append('file', file);
  
  const response = await fetch('http://127.0.0.1:8000/face/recognize_image', {
    method: 'POST',
    body: formData
  });
  
  return await response.json();
}
```

### WebSocket实时流示例
```javascript
const ws = new WebSocket('ws://127.0.0.1:8000/face/recognize_camera');
ws.onopen = () => {
  ws.send(JSON.stringify({ camera_id: 0 }));
};
ws.onmessage = (event) => {
  const data = JSON.parse(event.data);
  console.log('识别结果:', data);
};
```

## 🐛 常见问题

### 1. 模型文件缺失
系统需要以下模型文件（需要单独下载）：
- `models/face_detection/best.pt`
- `models/face_recognition/16_backbone.pth`
- `models/emotion_recognition/emotion_best.pt`

### 2. CUDA不可用
系统会自动切换到CPU模式，性能会有所下降

### 3. 端口被占用
修改 `settings.py` 中的 `PORT` 配置

## 📞 技术支持

如有问题，请查看API文档或联系开发团队。

---

**注意**：本系统仅供学习和研究使用，请遵守相关法律法规。
