# ⚙️ 人脸识别与表情识别系统 - 配置使用说明

## 📚 概述

本系统使用 `settings.py` 文件进行集中配置管理，所有重要的阈值、性能参数、安全设置等都集中在这个文件中。通过修改配置文件，可以灵活调整系统行为，无需修改代码。

## 🎯 主要配置分类

### 1. 识别阈值配置 (RECOGNITION_THRESHOLDS)

#### 人脸检测置信度阈值
```python
'face_detection_confidence': 0.5
```
- **作用**：控制人脸检测的严格程度
- **范围**：0.0 - 1.0
- **建议值**：0.5 - 0.8
- **调整建议**：
  - 提高阈值（如0.7）：减少误检，提高人脸质量，但可能漏检
  - 降低阈值（如0.3）：提高检测率，但可能包含低质量人脸
  - 当前值0.5：平衡检测率和质量

#### 人脸识别相似度阈值
```python
'face_recognition_similarity': 0.42
```
- **作用**：控制身份识别的严格程度
- **范围**：0.0 - 1.0
- **建议值**：0.4 - 0.7
- **调整建议**：
  - 提高阈值（如0.6）：提高识别准确性，减少误识别
  - 降低阈值（如0.3）：提高识别成功率，但可能增加误识别
  - 当前值0.42：平衡准确性和成功率

#### 表情识别置信度阈值
```python
'emotion_recognition_confidence': 0.6
```
- **作用**：控制表情识别的严格程度
- **范围**：0.0 - 1.0
- **建议值**：0.5 - 0.8
- **调整建议**：
  - 提高阈值（如0.8）：只返回高置信度的表情结果
  - 降低阈值（如0.4）：返回更多表情结果，但准确性可能降低
  - 当前值0.6：平衡准确性和覆盖率

### 2. 性能配置 (PERFORMANCE_CONFIG)

#### 视频处理帧间隔
```python
'video_frame_interval': 10
```
- **作用**：控制视频处理时每N帧处理一次
- **建议值**：5 - 20
- **调整建议**：
  - 值越小：处理越详细，但速度越慢
  - 值越大：处理越快，但可能错过一些人脸
  - 当前值10：平衡处理速度和准确性

#### 实时流处理帧间隔
```python
'stream_frame_interval': 5
```
- **作用**：控制实时流处理时每N帧处理一次
- **建议值**：3 - 10
- **调整建议**：
  - 值越小：实时性越好，但CPU占用越高
  - 值越大：性能越好，但实时性越差
  - 当前值5：平衡实时性和性能

#### 实时流处理间隔
```python
'stream_processing_interval': 100
```
- **作用**：控制实时流处理的频率（毫秒）
- **建议值**：50 - 200
- **调整建议**：
  - 值越小：延迟越低，但CPU占用越高
  - 值越大：CPU占用越低，但延迟越高
  - 当前值100：平衡延迟和性能

### 3. 文件处理配置 (FILE_PROCESSING_CONFIG)

#### 文件大小限制
```python
'max_image_size': 50 * 1024 * 1024,  # 50MB
'max_video_size': 500 * 1024 * 1024,  # 500MB
```
- **作用**：限制上传文件的最大大小
- **调整建议**：
  - 根据服务器内存和存储容量调整
  - 考虑网络传输时间
  - 平衡用户体验和系统资源

#### 支持的文件格式
```python
'supported_image_formats': ['.jpg', '.jpeg', '.png', '.bmp', '.tiff', '.tif']
'supported_video_formats': ['.mp4', '.avi', '.mov', '.wmv', '.flv', '.mkv']
```
- **作用**：定义系统支持的文件格式
- **调整建议**：
  - 添加新格式前确保OpenCV支持
  - 考虑格式的兼容性和性能

### 4. 安全配置 (SECURITY_CONFIG)

#### CORS设置
```python
'allowed_origins': ['*']
```
- **作用**：控制允许访问的源域名
- **调整建议**：
  - 生产环境建议限制为特定域名
  - 开发环境可以设置为 `['*']`

#### 请求频率限制
```python
'rate_limit': {
    'requests_per_minute': 60,  # 每分钟最大请求数
    'burst_size': 10            # 突发请求最大数量
}
```
- **作用**：防止API滥用
- **调整建议**：
  - 根据服务器性能调整
  - 考虑用户体验需求

## 🔧 配置调整步骤

### 1. 修改配置文件
```bash
# 编辑配置文件
nano settings.py
# 或者
vim settings.py
```

### 2. 调整参数值
```python
# 例如，提高人脸检测阈值
RECOGNITION_THRESHOLDS = {
    'face_detection_confidence': 0.7,  # 从0.5提高到0.7
    # ... 其他配置保持不变
}
```

### 3. 重启服务器
```bash
# 停止当前服务
Ctrl+C

# 重新启动
python3 start_server.py
```

## 📊 配置调优建议

### 提高识别准确性
```python
RECOGNITION_THRESHOLDS = {
    'face_detection_confidence': 0.7,      # 提高人脸检测阈值
    'face_recognition_similarity': 0.6,    # 提高人脸识别阈值
    'emotion_recognition_confidence': 0.8, # 提高表情识别阈值
}
```

### 提高识别覆盖率
```python
RECOGNITION_THRESHOLDS = {
    'face_detection_confidence': 0.3,      # 降低人脸检测阈值
    'face_recognition_similarity': 0.3,    # 降低人脸识别阈值
    'emotion_recognition_confidence': 0.4, # 降低表情识别阈值
}
```

### 优化性能
```python
PERFORMANCE_CONFIG = {
    'video_frame_interval': 20,            # 增加视频处理帧间隔
    'stream_frame_interval': 10,           # 增加实时流处理帧间隔
    'stream_processing_interval': 200,     # 增加处理间隔
}
```

### 提高实时性
```python
PERFORMANCE_CONFIG = {
    'video_frame_interval': 5,             # 减少视频处理帧间隔
    'stream_frame_interval': 3,            # 减少实时流处理帧间隔
    'stream_processing_interval': 50,      # 减少处理间隔
}
```

## ⚠️ 注意事项

### 1. 阈值调整的影响
- **提高阈值**：提高准确性，但可能降低覆盖率
- **降低阈值**：提高覆盖率，但可能降低准确性
- **建议**：根据具体应用场景平衡这两个指标

### 2. 性能配置的影响
- **减少处理间隔**：提高实时性，但增加CPU占用
- **增加处理间隔**：降低CPU占用，但降低实时性
- **建议**：根据服务器性能和用户需求调整

### 3. 文件大小限制
- **过大的限制**：可能导致内存不足或处理超时
- **过小的限制**：可能影响用户体验
- **建议**：根据服务器配置和网络环境调整

### 4. 安全配置
- **生产环境**：建议启用API密钥验证，限制CORS
- **开发环境**：可以放宽安全限制，便于调试

## 🔍 配置验证

### 1. 检查配置加载
启动服务器时查看控制台输出，确认配置正确加载：
```
正在加载模型...
正在加载人脸识别模型...
数据库构建完成，包含 X 个人
所有模型加载完成！
```

### 2. 测试阈值效果
使用测试图片验证阈值调整的效果：
- 调整人脸检测阈值后，测试不同质量的人脸图片
- 调整人脸识别阈值后，测试已知人员的识别结果
- 调整表情识别阈值后，测试不同表情的识别结果

### 3. 监控性能指标
- 观察处理速度和CPU占用
- 检查内存使用情况
- 监控识别准确性和覆盖率

## 📝 配置备份

建议在修改配置前备份原文件：
```bash
# 备份配置文件
cp settings.py settings.py.backup

# 如果需要恢复
cp settings.py.backup settings.py
```

## 🎯 总结

通过合理调整 `settings.py` 中的配置参数，可以：
1. **优化识别性能**：调整阈值平衡准确性和覆盖率
2. **控制资源使用**：调整性能参数控制CPU和内存占用
3. **提升用户体验**：调整文件限制和处理间隔
4. **增强系统安全**：配置CORS和频率限制

记住：修改配置后必须重启服务器才能生效！
